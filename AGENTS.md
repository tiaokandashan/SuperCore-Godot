# SuperCore Godot Agent Map

UserName:

本文件供 Codex 使用，是 `E:\UnityProject\SuperCoreGodot` 的全局工作规范和 AI 文档索引。模块细节、方案和审查内容不在本文件展开，统一到 `Docs` 文档库中查阅。

## 项目说明

- 总根目录是 `E:\UnityProject\SuperCoreGodot`。
- Godot 4.7.1 .NET 项目位于 `Project`。
- Unity 框架参考源是整个 `E:\UnityProject\SuperCore`；主框架代码入口是 `Assets/SuperCore`，可借鉴的 AI 文档位于 `ProjectDocs`，根 AI 入口是其 `AGENTS.md`。
- 根迁移任务是 `SCG-20260806-001`；SuperCore 启动场景、Module 生命周期基础、特性驱动发现和 12 个 Module 启动骨架已建立，具体 Module 功能尚未迁移。

## AI 文档入口

- 每次请求固定读取：`Docs/00_AI快速入口.md`。
- 通用模块路由：`Docs/01_项目总览/模块索引.md`。
- 编辑器工具路由：`Docs/01_项目总览/编辑器工具索引.md`。
- 业务路由：`Docs/01_项目总览/业务索引.md`。
- 任务路由：`Docs/01_项目总览/任务索引.md`。
- 工作流程、任务元数据、编码、目录、文档维护和日志规则按快速入口的条件表进入，不在本文件重复。

## 工作规则

- 所有请求先判断增、删、改、查；查询只读结束，修改在最终方案形成后等待用户独立消息精确回复 `确认实现`。
- 有效确认后才创建任务编号、责任人、索引和结构化日志。
- 除根任务自身外，所有迁移任务必须填写 `root_task: SCG-20260806-001`，并按真实前置关系填写 `depends_on`。
- 开始新任务、上下文恢复或范围扩散时，重新读取本文件、快速入口和相关当前规范。
- 修改前读取实际文件、调用点和未提交差异；当前实现优先于历史对话、任务记录和旧方案。
- 未经明确要求，不执行正式导出、发布、远端操作、破坏性清理或 Unity 迁移源写入。
- 待补充文档只保留路由，不代表对应框架、Module、业务或编辑器能力已经实现。

## 用户名解析

1. `UserName:` 非空时直接使用。
2. 为空时，在项目根只读执行 `git config --get user.name`。
3. 结果仍为空时，在创建任务编号前要求用户手动提供。
4. 不执行 `git pull`，不访问远端，不修改 Git 配置，也不自动写回本文件。

## 项目硬边界

- `Project` 是唯一 Godot 项目目录，`Docs` 不进入 Godot 资源导入范围。
- 已删除的旧 Godot 迁移全部失效，不恢复、不兼容，也不采用其验证结论。
- Unity 工程整体作为当前框架参考源只读使用；迁移时必须重新检查实际源码、配置、资源、工具和调用点，并按需参考其 AI 文档。
- Unity 当前代码、配置和资源的事实优先于 `ProjectDocs`；AI 文档只用于定位、职责、历史取舍和验证经验。
- 一次只迁移一个可独立验证的能力；当前阶段失败时不叠加后续阶段。
- 只修改任务直接相关文件，不为尚未迁移的能力创建空实现或兼容层。`SCG-20260810-003` 是用户明确授权的 12 个 Module 与模块自有配置空骨架例外；这些骨架不代表功能已迁移。

## 详细规范唯一来源

- 代码规则：`Docs/01_项目总览/编码规范.md`。
- 文件和文档：`Docs/01_项目总览/文档维护规范.md`。
- 请求与确认：`Docs/01_项目总览/工作流程规范.md`。
- 任务身份和关系：`Docs/01_项目总览/AI文档规范.md`。
- 任务日志：`Docs/01_项目总览/日志规范.md`。
- 目录边界：`Docs/01_项目总览/目录职责与修改边界.md`。
